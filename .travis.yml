language: c

os:
  - linux
  - osx

compiler:
  - clang
  - gcc

addons:
  apt:
    packages:
      - cmake
      - gnupg
      - valgrind
  homebrew:
    packages:
      - cmake
      - gnupg

before_script:
  - mkdir build
  - cd build
  - cmake ..

script:
  - ./run-build.sh
